# Task 02 — Checkout from GitLab (inside Docker)

## Description
Configure Jenkins (running in Docker) to checkout `sst-tr-api` from GitLab using appropriate authentication (SSH or access token).

## Expected Outcome
Code is successfully pulled into the Docker-based Jenkins workspace.