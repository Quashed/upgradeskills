pipeline {
    agent any

    stages {
        stage('Checkout hello-demo') {
            steps {
                git branch: 'main',
                    credentialsId: 'github-upgradeskills-https',
                    url: 'https://github.com/Quashed/hello-demo.git'
            }
        }
    }
}