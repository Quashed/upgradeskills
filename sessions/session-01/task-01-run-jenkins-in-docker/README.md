# Task 01 — Run Jenkins in Docker

## Description
Run Jenkins inside a Docker container and access the UI locally.

## Expected Outcome
- Jenkins running in Docker
- Jenkins UI accessible locally
- Ready for job creation  